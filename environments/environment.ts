export const environment = {
  production:false,
  URL_BACKEND: 'http://localhost:8080',
  URL_BACKEND_AUTH: '',
  
  // =========================================================================
  // MOCK BACKEND (MODO SIMULADOR):
  // Cambia esto a 'false' cuando ya tengas el backend real disponible en http://localhost:8080.
  // Esto desactivará los datos falsos y la aplicación se conectará normalmente.
  // =========================================================================
  useMockBackend: false
};